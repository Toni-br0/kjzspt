<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pearadmin.modules.report.mapper.ReportAutoCreateLogMapper">

    <select id="getListByWhere" resultType="com.pearadmin.modules.report.domain.ReportAutoCreateLog">
        select t.*,s.real_name create_user_name
         from report_auto_create_log t
             left join sys_user s on s.user_id =t.create_user_id
        where 1=1
        <if test="loginUserId != null and loginUserId != ''">
            and t.create_user_id = #{loginUserId}
        </if>

        <if test="reportAutoCreateLog.reportName != null and reportAutoCreateLog.reportName != ''">
            and t.report_name like concat('%',#{reportAutoCreateLog.reportName},'%')
        </if>

        order by t.create_time desc

    </select>

</mapper>